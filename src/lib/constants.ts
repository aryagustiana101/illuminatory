import { env } from "~/env";

export const APP_ENV = env.NEXT_PUBLIC_APP_ENV;
export const APP_URL = env.NEXT_PUBLIC_APP_URL;
export const APP_NAME = env.NEXT_PUBLIC_APP_NAME;
export const APP_DEVELOPER = env.NEXT_PUBLIC_APP_DEVELOPER;
export const APP_GITHUB_REPO_URL = env.NEXT_PUBLIC_APP_GITHUB_REPO_URL;
